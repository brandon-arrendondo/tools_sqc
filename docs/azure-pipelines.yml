# aurora-lint Static Analysis - Azure DevOps Example Pipeline
#
# This pipeline runs aurora-lint CERT C analysis on your C/C++ codebase.
# - On PRs: analyzes only changed files (--diff mode)
# - On push to main: full repository scan
# - Publishes SARIF results as a build artifact
#
# Prerequisites:
#   - Rust toolchain installed on the agent (or use a Rust container)
#   - A aurora-lint-rules.toml manifest (or use built-in defaults)

trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  CARGO_TERM_COLOR: always

stages:
  - stage: Build
    displayName: Build aurora-lint
    jobs:
      - job: BuildAuroraLint
        displayName: Build aurora-lint binary
        steps:
          - task: Cache@2
            inputs:
              key: 'cargo | "$(Agent.OS)" | Cargo.lock'
              restoreKeys: |
                cargo | "$(Agent.OS)"
              path: $(Pipeline.Workspace)/.cargo
            displayName: Cache Cargo

          - script: |
              cargo build --release
            displayName: Build aurora-lint

          - task: PublishPipelineArtifact@1
            inputs:
              targetPath: target/release/aurora-lint
              artifactName: aurora-lint-binary
            displayName: Publish aurora-lint binary

  - stage: Analyze
    displayName: Run Analysis
    dependsOn: Build
    jobs:
      - job: AnalyzePR
        displayName: Analyze PR (diff only)
        condition: eq(variables['Build.Reason'], 'PullRequest')
        steps:
          - checkout: self
            fetchDepth: 0  # Full history for --diff mode

          - task: DownloadPipelineArtifact@2
            inputs:
              artifactName: aurora-lint-binary
              targetPath: $(Build.SourcesDirectory)
            displayName: Download aurora-lint binary

          - script: |
              chmod +x aurora-lint
              ./aurora-lint . --diff \
                --min-severity Medium \
                --fail-on-severity High \
                --export results.sarif
            displayName: Run aurora-lint (diff mode)

          - task: PublishBuildArtifacts@1
            inputs:
              pathToPublish: results.sarif
              artifactName: sarif-results
            displayName: Publish SARIF results
            condition: always()

      - job: AnalyzeFull
        displayName: Full Analysis
        condition: ne(variables['Build.Reason'], 'PullRequest')
        steps:
          - checkout: self

          - task: DownloadPipelineArtifact@2
            inputs:
              artifactName: aurora-lint-binary
              targetPath: $(Build.SourcesDirectory)
            displayName: Download aurora-lint binary

          - script: |
              chmod +x aurora-lint
              ./aurora-lint . -d . \
                --min-severity Medium \
                --fail-on-severity High \
                --export results.sarif
            displayName: Run aurora-lint (full scan)

          - task: PublishBuildArtifacts@1
            inputs:
              pathToPublish: results.sarif
              artifactName: sarif-results
            displayName: Publish SARIF results
            condition: always()
