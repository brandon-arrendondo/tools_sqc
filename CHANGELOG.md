# Changelog

All notable changes to aurora-lint are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

**Versions here are published releases**, not every version the crate passed
through. aurora-lint bumps the patch version on nearly every commit, and only tagged
versions are built, signed and published; a section therefore covers every
change since the previous release, not since the previous version number.

This file is generated by `scripts/generate_changelog.py` from the project's
task database. Edit a task, not this file -- a hand edit is overwritten on the
next regeneration, and CI checks the two agree.

## [Unreleased]

### Fixed

- INT30-C/INT31-C/INT32-C/ARR30-C: pointer arithmetic misclassified as integer arithmetic
- INT34-C: flags almost any non-literal shift amount with no value-range reasoning (0/104 TP on sel4)
- ARR36-C: '*p' is treated as a pointer whatever p points to, so '*s1 - *s2' on two char* reads as pointer subtraction
- ARR30-C: three location-attribution bugs (wrong array size, wrong line, conflated #ifdef branches)
- ARR36-C: a flexible-array member IS storage, but storage inside the array the other operand walks

### Changed

- Publish aurora-lint to crates.io (coordinator-node only, holds the publish credentials)
- Rename the GitHub repo tools_sqc -> aurora-lint, then point Cargo.toml repository/homepage at it

## [0.4.336] - 2026-09-06

### Added

- compile_commands: recover the compiler's implicit system include dirs (cc -E -Wp,-v -)
- Nothing detects a two-node Cargo.toml version collision (silent, third occurrence)
- FIO30-C and FLP03-C never merge ProjectContext cross-file macro data -- file-local collect only

### Fixed

- DCL31-C: attribute macros (VISIBLE/ALIGN/SKIM_BSS) between type specifier and declarator misdetected as missing-type-specifier
- Misc small rule bugs from hostap audit (16-40 bucket): STR00-C/EXP36-C/DCL31-C/WIN04-C/INT30-C/EXP40-C/INT14-C
- DCL31-C false 'no prior declaration' on same-file, prior-defined function: real parser interaction bug (tlsv1_server_read.c)
- MEM31-C: no cross-file global-variable value-type awareness (misses seL4's current_lookup_fault/current_fault class)
- EXP33-C ifdef-correlation doesn't cover reads whose #ifdef guard was blanked by task 647's label-preproc parser fix
- EXP34-C doesn't model NORETURN-attributed calls as terminating control flow
- INT10-C/INT32-C: multi-level typedef chain (word_t family) not resolved on seL4
- MEM30-C: sibling if-without-else early-exit branches cross-flagged as double-free
- Adjudicate MSC10-C findings into ground_truth (new rule, no labeled tuples exist)
- INT10-C: enum-constant modulo operand not recognized as non-negative (seL4 int_irq_min)
- EXP33-C: misc smaller FP categories from delta-adjudication (categories 5/8/9/10)
- FLP05-C/FIO47-C/INT15-C/INT13-C/INT08-C/POS39-C/INT01-C: lower-confidence candidates from task-389 sweep, need individual verification
- INT32-C: BIT() macro expansion not recognized as unsigned (seL4 statedata.c)
- MSC04-C cross-TU static-name-collision noise surfaced by task 297 migration
- DCL31-C: function-pointer parameters and locals not recognized as declared callables (all 32 new sqlite findings)
- DCL31-C silently disabled project-wide by one unresolvable system header (is_missing_project_header misclassifies /usr/include subdirs)
- VRA treats a function-local `static` initializer as its value on every call
- sqlite fts5TestUtf8: 4-byte branch never validates z[i+3], advances i+=3 not 4
- MSC17-C: case label mis-pairing near adjacent #ifndef/#endif boundaries
- API00-C: NULL-tolerant external-API sinks (dbus_set_error) drive ~62 hostap FPs
- MSC13-C: same-scope declarations split across #if/#else counted as unused (65 of 74 sample FPs)
- ARR38-C: is_potentially_user_controlled_in_source never looks at the destination buffer (100% FP on the task-682 delta)
- INT13-C: both shift operands checked, and operand name taken from first identifier in subtree (12 of 18 sample FPs)
- ARR00-C: find_pointer_source_array_recursive has two text-scan bugs (all 8 sample FPs; rule sits at 0.4% precision)
- ARR36-C: fires on non-pointer operands (float/int comparisons reported as pointer subtraction)
- INT10-C: guard-bounded local int not recognized as non-negative in modulo (seL4 intc.c)
- API00-C: only canonical type-max comparisons count as validation, so ordinary guards are missed
- INT10-C/parser: cast-to-typedef mis-parsed as bitwise-AND when typedef isn't scanner-visible (seL4 lock.c)
- API00-C: in-function NULL guard missed in long/goto-heavy bodies (2 sqlite rows)
- ARR38-C: has_size_validation matches seven literal substrings, so ordinary guards are missed
- INT10-C: a non-negative DIVISOR suppresses the finding, contradicting the rule's own dividend-only reasoning
- MSC13-C: three smaller FP classes (unbraced switch-case decls, switch CFG edges, macro-hidden uses)
- EXP34-C/EXP33-C: os_free missing from null-tolerant callees, and output-param init still unrecognized (3 sample FPs)
- INT08-C: narrow-type operands promote to int, so the flagged arithmetic cannot overflow (all 6 sample FPs; rule at 0.0% precision)
- bench: realworld_runs identity migration aborts on legacy duplicate run_ids, killing ALL local realworld ingest
- Juliet: ENV33-C/ENV03-C/STR02-C gained 622 FP with identical TP between 0.4.321 and 0.4.332 (bisect it)
- ARR00-C: pointer-subtraction check has no type awareness, so scalar arithmetic is flagged
- ARR36-C: one base compared against itself under two spellings (param:X vs X, x vs p->x)
- MSC04-C: reported cycle path is nondeterministic run-to-run, and find_cycle's visited set is never unwound on backtrack
- MSC04-C (and INT08-C) emit nondeterministic message text for an identical finding
- INT32-C: same inverted narrow-operand premise as INT08-C -- 'char + char' flagged as possible signed overflow
- bench compare: rule rows fabricate base_tp/base_fp 0 for any rule outside the base run's top-20-by-FP
- INT32-C: bare sizeof/single-variable malloc args flagged as overflowing arithmetic; realloc arg1 (pointer) checked as if it were the size arg
- ARR36-C: every pointer parameter gets a unique synthetic base, so any end-minus-pos is a violation (0.1% precision)
- ARR30-C: 'unvalidated function parameter index' doesn't recognize caller-side validation (seL4 decode/invoke split, size-parameter-defines-bound convention)
- Generated fixture tests build context via prescan_single_tree, which no production path uses -- 17 findings on 11 pass fixtures the tests call clean
- ARR30-C: 'unbounded while loop with pointer increment' doesn't recognize NUL-terminated string / NULL-terminated pointer-array scans
- CON40-C, EXP33-C and MSC13-C are each cubic in block-nesting depth; MEM30-C's 2000-level pass fixture hangs a full-rule-set scan
- ARR36-C: a field-path base is treated as naming storage, so a pointer-valued struct member reads as its own array
- int_provenance risky-operand gate fails open on empty function_summaries, so 56 INT30/31/32-C fail fixtures have been passing on the no-context fallback, not on the rule
- MEM31-C learns wrapper allocators from prescan but not a void** free wrapper, so safe_free() callers leak 5 findings under -d
- ARR30-C: caller-side index validation is file-local, so a callee whose only callers live in another file stays flagged
- ARR30-C: 'unbounded while loop with pointer increment' still fires on flag/counter conditions, bare producer calls and ctype predicates
- ARR36-C: the caller-side proof for parameter pairs is file-local, so a cross-file caller cannot be read
- ARR36-C: an untracked bare pointer variable's raw name is used as a base, so a pointer with an unknown target reads as its own array
- ARR36-C: 'typedef struct X Alias;' leaves Alias unresolvable, so a member reached through it falls back to storage-naming

### Changed

- INT30-C/INT31-C likely have the same missing float-operand filter as INT32-C had (task 643)
- sqc's --rules CLI flag doesn't gate rule execution, only filters printed output
- VRA per-query replay is O(n) per call, making any VRA-consuming rule O(n^2) on large basic blocks
- INT32-C's has_function_level_overflow_check_scoped is O(n) per candidate, separately quadratic from task 669
- Benchmark pass with MSC10-C enabled (new rule, task 337)
- MSC17-C: severe performance issue on large files (sqlite src/vdbe.c ~98s)
- Delta-adjudicate sel4 INT10-C/INT32-C after task 657's typedef-chain fix
- EXP08-C: decide whether the ALL_CAPS-macro exclusion should actually fire
- ARR30-C/ARR38-C: scope the unread ~3500 lines of bounds-checking logic
- ARR30-C: fix missing word-boundary anchors in has_function_parameter_bounds_check/has_lower_bound_check/has_recursive_index_modification
- DCL13-C should exclude function-pointer-typed parameters
- Measure --system-includes on the real-world corpus (task 623 follow-up)
- NAS-2: confirm the first successful pg_dump since the grant fix, and find the true last-good backup date
- ARR38-C: delete dead check_three_arg_size
- ARR30-C: fix find_matching_param_declaration's raw substring match to token-boundary
- ARR30-C: dedup identifier-substring bounds-check heuristic (has_dynamic_bounds_check / check_while_loop_pointer_increment)
- ARR30-C: dedup collect_param_decode_buffers / const_char_ptr_params_without_length
- ARR38-C: scope is_unvalidated_function_parameter/has_size_validation to containing function
- ARR38-C: scope is_type_size_mismatch's array-declaration search to containing function
- ARR38-C: remove/generalize 'struct obj' Juliet-specific hack in check_io_function
- sqlite scope: audit ext/ tree for more test-only files missing the test_*.c glob
- Upgrade installed knots 1.0.0 -> 1.16.0, then use AICP to target interprocedural-analysis work
- Upstream sqlite: fts5TestUtf8 never validates the 4th continuation byte and advances i by 3 not 4
- INT31-C: get_type_width() doesn't recognize float/double, so double->int narrowing conversions are silently missed
- mosquitto scope: add include/ + config.h to data/benchmark_repos.json and the precision_audit README, exclude the C++ wrapper by name
- sqlite scope 656: check oracle for labels on the 7 newly-excluded paths
- Drop the '// sqc-test: prescan' gate so every fixture is tested under the context the scan builds (62 fixtures fall over when it lands)

### Documentation

- Consolidate benchmark data sources: generate presentation docs from data/benchmarks.db instead of hand-maintained files
- docs: record why 4 rules are tracked but not implemented (2 parked on incomplete CERT content)
- README 'Alternatives' section: prose contradicts the table 909/910 just corrected, plus an unattributed timing comparison
- Verify 99.2%/81.7% clang-tidy precision pair: computed over 16 or 15 Juliet CWEs?

## [0.4.315] - 2026-09-01

### Fixed

- tree-sitter-c ERROR-cascade: label immediately followed by #ifdef mis-parses guarded statements

### Changed

- API00-C rule fires on integer-overflow findings, not just null-pointer checks
- Install scripts/cargo-target-gc.sh as an hourly cron on r720 (and any other dev node)
- INT32-C missing float-operand filtering (like INT33-C/FLP06-C already have)
- Onboard 2 new same-VLAN nodes for parallel benchmark writes

## [0.4.314] - 2026-09-01

_No user-visible changes; see the commit log for internal work._

## [0.4.311] - 2026-09-01

### Added

- Implement DCL42-C: Only call functions with the unsequenced or reproducible attributes if they actually have the asserted property
- Implement FLP38-C: Avoid undefined behavior while using type-generic macro functions
- Implement MSC00-C: Compile cleanly at high warning levels
- Implement MSC01-C: Strive for logical completeness
- Implement MSC05-C: Do not manipulate time_t typed values directly
- Implement MSC06-C: Beware of compiler optimizations
- Implement MSC09-C: Character encoding: Use subset of ASCII for safety
- Implement MSC11-C: Incorporate diagnostic tests using assertions
- Implement MSC14-C: Do not introduce unnecessary platform dependencies
- Implement MSC15-C: Do not depend on undefined behavior
- Implement MSC17-C: Finish every set of statements associated with a case label with a break statement
- Implement MSC20-C: Do not use a switch statement to transfer control into a complex block
- Implement MSC21-C: Use robust loop termination conditions
- Implement MSC22-C: Use the setjmp(), longjmp() facility securely
- Implement MSC24-C: Do not use deprecated or obsolescent functions
- Implement MSC10-C: Character encoding: UTF8-related issues
- STR31-C: Juliet CWE-122 regression -4 TP/+9 FP (v0.4.277-283, unrelated to task 604)
- Implement MSC23-C: Beware of vendor-specific library and language differences

### Fixed

- EXP34-C/ARR00-C output-arg recognition: both macro (task 195) and cross-file function (task 323/319) output-args
- MSC13-C: read-detector misses loop-condition/goto-cleanup/trailing-return reads (8% real-world precision)
- MSC12-C: fix grouped case-label bodies misread as empty/no-effect (0% real-world precision)
- Fix API01-C detection gap exposed by new wiki fixtures
- Fix ARR37-C detection gap exposed by new wiki fixtures
- Fix CON07-C detection gap exposed by new wiki fixtures
- Fix CON09-C detection gap exposed by new wiki fixtures
- Fix CON34-C detection gap exposed by new wiki fixtures
- Fix DCL02-C detection gap exposed by new wiki fixtures
- Fix DCL04-C detection gap exposed by new wiki fixtures
- Fix DCL07-C detection gap exposed by new wiki fixtures
- Fix DCL13-C detection gap exposed by new wiki fixtures
- Fix DCL17-C detection gap exposed by new wiki fixtures
- Fix DCL37-C detection gap exposed by new wiki fixtures
- Fix DCL40-C detection gap exposed by new wiki fixtures
- Fix ENV34-C detection gap exposed by new wiki fixtures
- Fix ERR33-C detection gap exposed by new wiki fixtures
- Fix EXP00-C detection gap exposed by new wiki fixtures
- Fix EXP36-C detection gap exposed by new wiki fixtures
- Fix EXP42-C detection gap exposed by new wiki fixtures
- Fix EXP45-C detection gap exposed by new wiki fixtures
- Fix FIO10-C detection gap exposed by new wiki fixtures
- Fix FIO34-C detection gap exposed by new wiki fixtures
- Fix INT09-C detection gap exposed by new wiki fixtures
- Fix INT34-C detection gap exposed by new wiki fixtures
- Fix MEM31-C detection gap exposed by new wiki fixtures
- Fix MSC12-C detection gap exposed by new wiki fixtures
- Fix MSC13-C detection gap exposed by new wiki fixtures
- Fix MSC37-C detection gap exposed by new wiki fixtures
- Fix SIG34-C detection gap exposed by new wiki fixtures
- Fix STR34-C detection gap exposed by new wiki fixtures
- Medium-tier FP cleanups: EXP33-C, FIO42-C, ARR39-C, WIN04-C
- EXP36-C: don't flag alignment-increasing casts to STRUCT_PACKED destination structs
- MEM30-C: conflates unrelated switch arms as a single free-then-use path
- MEM30-C: misattributes free() of the 2nd arg to the 1st arg (ap_free_sta-style calls)
- MEM30-C: trusts FunctionSummary.frees_params as MUST-free when it's really MAY-free (conditional/transitive frees)
- MEM30-C: flags a multi-arg free call's own freed argument as UAF within the same call
- MEM30-C: nullified_vars union in if/switch merge masks a freed_vars hit from the SAME merge
- ARR30-C: inconsistent gamepad-accessor flagging (recall/FN gap)
- Cross-function variable-name conflation: EXP39-C confirmed, ARR30-C confirmed, likely recurring architectural pattern
- EXP30-C doesn't recognize && / || as C sequence points
- FIO47-C: get_expected_type doesn't distinguish scanf-family (pointer args) from printf-family (value args)
- MEM30-C: triple-pointer out-param (char ***) confuses UAF tracking
- MEM31-C false double-free: reassignment via user-defined allocator wrapper doesn't clear freed-state
- ARR38-C: whole-file buffer_info map conflates same-named buffers across functions
- CON04-C: whole-file thread_creations/joined_or_detached maps conflate same-named thrd_t across functions
- CON30-C: whole-file tss_keys/tss_set_calls/tss_get_freed maps conflate same-named TSS keys across functions
- DCL39-C: whole-file struct_vars map can misjudge trust-boundary leak across functions
- EXP43-C: whole-file pointer_bases map can misjudge restrict-pointer aliasing across functions
- FIO01-C: whole-file file_name_vars map merges unrelated functions' FILE*/fd timelines
- FIO05-C: per-function loop is dead code, falls through to whole-file file_operations map
- FIO13-C: whole-file file_operations map produces false successive-ungetc across functions
- FIO24-C: open_files/file_pointers maps persist across functions despite per-function loop
- FIO50-C: analyze_scope runs once over whole translation_unit, not per function
- INT10-C: whole-file type_map conflates same-named vars across functions
- INT30-C: whole-file type_map conflates same-named vars across functions
- MEM02-C: whole-file var_types map conflates cast-type checks across functions
- POS53-C: whole-file cond_var_to_mutexes map falsely aggregates mutexes across functions
- STR32-C: whole-file unsafe_arrays/array_locations can mask real missing-null-termination across functions
- EXP36-C ignores STRUCT_PACKED when computing alignment-increase
- ARR30-C: recognize recv()/read()-bounded return value as a safe array index
- FN-detection 2b: OOM null-deref after sqlite3_malloc* (EXP34 pattern)
- FN-detection 2c: integer truncation before allocation (INT31 pattern)
- Fix API05-C: unsound trigger (any size_t -> flag any pointer); then advisory triage
- Detect warning-without-return guards (e.g. overflow check that logs but does not return) -> stack/buffer OOB FN class
- Tree-sitter-c parse failure on raylib rlgl.h: RLAPI-style empty-expansion macro before declarations cascades ERROR nodes
- STR00-C/INT07-C/FLP30-C: systematic wrong-type-attribution (claims char/float for actual int variables)
- ARR30-C: macro-named single-sided bound guard (idx < MACRO_NAME) not recognized as a proper bounds check
- EXP40-C: const_vars tracked whole-file, not per-function (cross-function conflation)
- MEM30-C misattributes free() to wrong argument in multi-pointer-arg calls (ap_free_sta-shaped)
- Triple-pointer out-params (T ***) confuse UAF/type tracking
- DCL30-C: over-broad 'local pointer assigned through out-param' heuristic flags heap-derived pointer values
- INT09-C: enum MAX-sentinel const-eval mis-resolves NUM_X - 1 idiom to 0
- DCL40-C misparses trailing STRUCT_PACKED macro as a redeclared object
- INT09-C can't resolve #define macro-tag arithmetic in enum initializers (CURLINFO_STRING + N idiom)
- INT09-C (and enum-consuming rules generally): multi-line attribute-macro between enumerator name and initializer corrupts enumerator_list parse
- Triage list: misc real sqc analyzer bugs found during hostap audit (16-40 bucket)
- FIO30-C: cross-TU + function-pointer format-string FPs (remaining 210)
- STR31-C cross-function FP follow-ups: function-pointer (44), static-global (45), control-flow (21/22), CWE-127 source-side
- Upstream contribution: report/PR genuine findings back to benchmarked OSS projects
- CWE-123 (Write-What-Where) has 0% flaw-hit detection across its whole rule set
- unknown_identifier_recovery blanks a real #endif in mosquitto's uthash.h (ERROR-recovery over-blanking)
- STR02-C: call-site-const param tracking + adjudicate hostap SQL findings (task 8 follow-up)
- STR02-C: 2 residual FP shapes found adjudicating hostap (task 469 follow-up)
- ARR38-C: cross-file CWE805 per-call-site buffer-size bucket (task 206 follow-up)
- Find an 8th real-world oracle codebase that abides by MSC12-C's no-empty-body style
- MSC12-C: recognize while(cond);/for(;;); busy-wait polling idiom as intentional (empty control-flow, not switch-case)
- MSC12-C: attribute-macro-decorated declaration (BOOT_BSS-style) misattributed as a discarded-expression statement
- MSC12-C: no way to mark an empty body as intentional (no-op stubs, deliberate empty switch cases)
- Exports (JSON/SARIF/CSV/Excel) don't serialize requires_manual_review -- the '?' confidence marker is CLI-stdout-only
- MSC12-C: macro-token statements with hidden real effects (lock/barrier macros) flagged as no-effect
- ARR38-C: find_array_size/find_string_literal_length search whole file unscoped (cross-function false match risk)
- CON04-C/CON34-C/CON39-C: thread-spawn detection inconsistent, 2 of 3 miss pthread_create
- DCL30-C: is_alloc_expression substring-matches 'ALLOC' in 'DEALLOCATE', misclassifies deallocation as allocation
- INT32-C: local is_signed_type misclassifies 'long'/'long long'/'signed char' as not-signed
- MEM33-C: extract_assignment_expression false-matches '==' as '=' (confirmed bug)
- Third+ instances of the task-475 macro-detection anti-pattern: PRE32-C, PRE31-C, DCL31-C, ENV03-C
- CON34-C: is_allocation_call allocator list missing strdup (possible FN in concurrency lifetime tracking)
- EXP11-C: extract_struct_name doesn't strip trailing pointer '*', likely wrong on 'struct widget *'
- INT14-C: extract_param_name doesn't unwrap pointer/array declarators (pointer params return no name)
- MEM31-C/STR30-C: non-recursive declarator-identifier unwrap may miss doubly-nested declarators
- INT30-C: get_operator/get_assignment_operator missing modulo ('%'/'%=') that INT32-C's versions have
- API02-C misses size param on multi-line wrapped function declarations
- MSC21-C: fires on non-arithmetic loop shapes (pointer traversal, function-return conditions)
- DCL15-C: recognize export-macro-annotated header declarations as needing external linkage
- Investigate shared root cause: 6+ rules misfire on mosquitto's libmosquittopp.h (C rules on C++ header)
- MSC13-C: misparses C++ default-argument prototypes as variable stores
- WIN04-C: flags declaration lines with no function-pointer token, likely shares root cause with task 564
- PRE00-C false-positives on typecheck-gcc.h's deliberate multi-eval type-check macro idiom
- EXP33-C (and likely other init-state rules): flat per-function InitStateMap conflates same-named locals across sibling scopes
- EXP33-C: is_read_in_field_expression doesn't handle base.field[i]=val as a write
- EXP33-C: bare-pointer args to unknown functions never clear malloc-content-uninitialized state
- EXP33-C: is_subscript_read_context doesn't exclude address-of (&arr[i]) context
- EXP33-C: sqlite3_snprintf-style wrappers misindex their destination arg (category 4)
- EXP33-C: static-storage-duration variables never flagged content-unsafe (category 6)
- EXP33-C: assignment-in-condition / short-circuit-guaranteed reads (category 7)
- Adversarial re-verify pure-ftpd SQL-client TP/FP labels (task 301 follow-up)
- API00-C: dispatch-table auth-backend callbacks still FP after task 169's internal-contract suppression
- ARR02-C: doesn't recognize initializer-inferred array size or function-parameter arrays
- ARR37-C: misfires on plain integer variables, not pointers
- DCL06-C: recognize self-evident bit-packing/unit-conversion literals to reduce magic-number FPs
- ERR01-C: check_errno_setting_functions fires under wrong rule ID (should be ERR30/ERR33-C territory)
- EXP34-C: misfires on plain integer variables, not pointers (likely shared root cause with task 556)
- EXP34-C: recognize SQLite's documented NULL-safe C-API surface
- EXP36-C: reports fabricated alignment value for byte-sized target types
- EXP39-C: unresolved function-parameter types silently default to int
- MEM10-C: don't flag a single plain 'if (ptr == NULL) return' guard clause
- Extend dead-code scanner to locally-constant #if defined(MACRO) branches
- ARR30-C: resolve flexible-array-member real size from allocation site, not declared size
- FN hunt: pure-ftpd SQL-client files (task 301 follow-up)
- INT08-C wiki_noncompliant_1.c: comment overclaims actual short-overflow at 32767 (rule only fires on narrow types)
- MSC12-C: seL4 AUXUPD/GHOSTUPD formal-verification annotation comments misread as empty if/else branches
- hostap ARR30-C: fix buffer mis-linking at p2p_supplicant.c:5132
- MSC12-C: __attribute__((__may_alias__)) typedef misparsed as bare-identifier no-effect statement
- ARR37-C: follow &arr[i] and output-parameter indirection to array origin
- STR34-C: fires on constant-value writes, not just sign-sensitive reads
- MSC04-C: function-pointer/callback dispatch fabricates spurious recursion cycles
- INT30-C/INT32-C: walk back one assignment hop for malloc size taint
- DCL31-C/MEM31-C: seL4 bitfield-codegen accessors misread as undeclared/leaking (0% precision, 190/490 sample findings)
- INT33-C macro-constant merge precedence is inverted vs. the other 8 const_eval consumers (project wins, not per-file)
- MEM30-C: mutually-exclusive branches and allocator-context arg both misread as freed
- MSC06-C: scope-exit heuristic ignores escaping/persisting buffers
- INT10-C: doesn't track operand's declared type through assignment chains
- DCL41-C: doesn't expand CASE()-style macros before switch structural analysis
- MSC17-C: fallthrough-comment recognition is broadly under-inclusive (19.8% batch precision)
- find_enclosing_declaration_for_identifier missed for-loop-declared variables (MSC13-C regression from task 386)
- MSC17-C: stray empty statement after a braced case body masks the nested break
- find_enclosing_declaration_for_identifier didn't resolve declarations nested inside #ifdef (MSC13-C regression from task 386, round 2)
- EXP33-C: curl's Curl_rand macro-forwarding-to-real-function not recognized as a write
- EXP33-C: #ifdef-both-branches join produces spurious MaybeUninitialized across unrelated guarded blocks
- MEM31-C: field-expression allocation targets bypass the value-only-local pointer-evidence guard (task 580 gap)
- Investigate API00-C/API05-C precision (surfaced by task 625's overlap analysis)
- MSC24-C: sscanf incorrectly on banned-function list + dead-#ifdef-branch FPs
- MEM30-C: goto/label mutual-exclusion gap (forward goto past a loop)
- MSC17-C: terminator check doesn't see break/return hidden inside nested #ifdef branches of a case body

### Changed

- Publish next crates.io release (v0.4.120+)
- MEM31-C ownership model
- Extend cross-file output-param recognition (modifies_params) to EXP34-C null_state + ARR00-C array-init, mirroring EXP33-C's fix
- MEM01-C classify_stmt_for_ptr: pointer reassignment inside a while/for loop CONDITION not recognized (only top-level expr-statement assignment is)
- MEM01-C (and other CFG-based rules): switch_statement bodies are CFG-opaque, hiding free()+return/break inside case arms
- EXP33-C get_var_info_at_with_config: no replay for reads within a still-in-progress short-circuited if/while/for condition (narrower than originally thought; MEM01-C's #321 is separate)
- MEM01-C: interprocedural &ptr output-param awareness (avoid coarse any-call heuristic)
- CON34-C/CON40-C: byte-position/expression-depth pruning leaves residual deep-nesting overflow risk
- Bespoke explicit-stack rewrites for ~15 stateful/branch-sensitive rule files (task 153 residue)
- Automate cargo publish in release CI
- Add crates.io discoverability metadata (keywords/categories/readme/rust-version)
- Decide: bump lang-parsing-substrate 0.3.0 -> 0.4.0
- EXP33-C + ARR00-C: cross-function output-buffer fill pattern (non-const ptr + return-value guard)
- Fix wiki parser to exclude output examples from test generation
- Audit/migrate scrape_cert_wiki.py for CERT wiki's move off Confluence to cmu-sei.github.io
- Full re-ingest of CERT C standard via the new JSON-payload scraper (task 325 follow-up)
- MSC12-C: 4 genuine misattribution bug fixes from the 0.4.143 postfix sample
- Implement CFG-based dead-store detection for MSC13-C (cross-branch unused value)
- Paper: add ground-truth methodology-limitations note (adjudication is a snapshot, not asymptotic)
- MEM31-C: gate goto-cleanup heuristic on callee's actual pointer return type
- Surface 'unlabeled fraction' as a first-class metric in realworld-score / compare_runs
- Process gate: require delta-adjudication before publishing precision/recall claims for a changed rule
- ARR30-C: Juliet TP-rate regression from the per-function buffer-scoping fix (task 239/389)
- MEM31-C: don't treat const-qualified/borrowed-accessor return values as allocations
- Generic 'unknown bare identifier before declaration' parse recovery (beyond locally-#defined empty macros)
- raylib rlgl.h: giant ERROR node persists at the file-wide header guard even after tasks 435+437 (needs real diagnosis, not more token-blanking)
- MEM30-C: ~26% analysis-time regression from LValue points-to migration
- MSC13-C: is_read_context blanket-fails init_declarator value-position reads (false dead-store)
- MSC13-C loses CFG connectivity across raw #if/#endif-split if/else chains (false dead-store)
- MSC13-C misses static-local cross-invocation reads (ring-buffer/lazy-init false dead-store)
- ARR30-C false positives: struct-field-guarded bounds checks and enum-constant array indices not recognized
- MSC13-C/CFG: switch-case labels split by #if/#endif also lose connectivity (dangling-else sibling bug)
- ARR30-C: unvalidated-param-index FP when dest buffer is calloc'd to a size scaled by that same index var
- ARR30-C: extend round-up-alloc-size proof to mod-based padding (rcore.c:3474 ComputeSHA256)
- MSC13-C: verify QOA-arm dead store in raudio.c UpdateMusicStream is a genuine TP
- Paper — real bug case study
- ARR30-C: extract inline state-tracking block out of check_with_buffer_info recursive dispatcher
- ARR30-C: audit other outsized functions for similar extract opportunities
- MEM31-C: check double-free tracking for cross-call-frame/recursion scoping bug (like CON30/POS53/STR32)
- lang-parsing-substrate 0.1.5 CFG builder not adopted: missing macro-constant folding + switch/goto
- C11/Annex-K auto-gating for relevance-detection (task 216 v2)
- Paper — taint tracking for CWE-78/CWE-89 (future work)
- Synthesize/refresh the 569 existing wiki_*.c CERT fixtures from the new site's fragment-based content (task 326 follow-up)
- Audit wiki_*.c fixtures for tree-sitter ERROR nodes (non-compiling literal fragments passing trivially)
- Build/verify a capability index for existing internal functions before writing new ones (avoid re-implementing existing infra)
- Design: shared call-role classification layer (allocator/printf-family/etc.) on top of std_functions.rs
- Promote INT30-C/INT32-C's ~15 shared overflow-detection helpers into one module
- FLP: 9 of 14 rules reimplement float-type detection instead of using float_typing.rs
- STR31-C: standalone ~2250-line text/line-based buffer-size engine, never adopted ARR00-C's AST resolver
- STR31-C Phase 1: dedup pointer-alias + fixed/alloca numeric sizing against buffer_size.rs
- ARR30-C/ARR38-C: dedicated fix-scoping pass for oversized buffer-size-resolution subsystems
- ARR30-C: delete self-duplicated find_identifier_in_declarator/extract_variable_name_from_declarator
- STR31-C/ARR00-C Phase 2: extract resolve_declared_array_size to shared module, wire STR31-C to it
- STR31-C Phase 1b: dedup find_fixed_alloc_size/find_alloca_size numeric branches against buffer_size::resolve_alloc_assigned_in_range
- buffer_size.rs: extend calculate_malloc_size with reversed sizeof(T)*N order; dedup ARR38-C onto it
- buffer_size::extract_sizeof_value substring-matches int64_t/uint64_t as 'int' (size 4) before checking 'long' (size 8)
- STR31-C find_fixed_alloc_size/find_alloca_size: evaluate adopting MAX-across-assignments semantics
- Migrate ARR30-C's is_alloc_call to call_roles::is_allocator_call once task 497's dedicated audit lands
- buffer_size.rs: add resolves_to_strlen_call primitive for indirect strlen/wcslen alloc sizing
- Consolidate 3+ divergent sizeof-type-size tables into one canonical function
- Delete dead Str31C::analyze_buffer_size
- FLP03-C: migrate is_fp_expression/contains_float_identifier to float_typing (structural rework, not a quick swap)
- FLP03-C: 0% measured precision across real-world oracle (821→534 all-FP after task 501 migration)
- MEM33-C: ~110 rule-local helpers over 3640 lines, heavy hand-rolled text heuristics -- needs dedicated audit
- MEM33-C: is_flexible_array_struct_target catch-all fallback flags nearly any short identifier
- MEM33-C: contains_pointer_syntax false-positives on array-size arithmetic containing '*', causing missed automatic-storage violations
- MEM33-C: is_likely_flexible_struct_pointer/variable use unbounded substring matching on 'flex'/'shared'/'global_'/'thread_' with no word boundaries
- MEM33-C: is_likely_flexible_struct_function_call flags almost any getter/factory function by name
- MEM33-C: check_invalid_type_casting is an inverted heuristic (relies on variable name, not type)
- MEM33-C: check_const_casting_specific decides const-violation from variable NAME containing 'const', not actual const qualifier
- MEM33-C: find_allocation_context scans the entire file unscoped (same bug class as task 496)
- MEM33-C: is_explicitly_declared_array/is_clearly_struct_array_pattern/array_contains_flexible_structs naming-heuristic cluster over-fires
- MEM33-C: check_offsetof_misuse/_in_init/_in_binary over-broad substring matching, plus hardcoded dead-idiom identifier names
- MEM33-C: is_flexible_member_access hardcodes 'data' only, ignoring the file's own broader (unused, dead_code) is_flexible_array_member_name
- Consolidate rule-local duplicated helpers into the shared utility layer (systemic sweep, task 479 follow-on)
- MEM33-C: check_node_for_variable_assignment/extract_assignment_expression can match inside comments in multi-line node text
- Fix MSC17-C: recurse into braced case-body/if-else terminators + recognize non-comment fallthrough markers
- Investigate upstream status: 11 sqlite ext/ OOM-NULL-deref bugs, file disclosure if unfixed
- Migrate to substrate's fixed is_parseable_extension API once available (low priority)
- Investigate upstream status: hostap UAF in wpas_dbus_handler_remove_interface, file disclosure if unfixed
- tools_sqc internal utility-layer improvements (ast_utils/points_to/etc.) — NOT the lang_parsing_substrate crate
- hostap upstream disclosure: verify real-bug list against current mainline, deep-dive survivors
- hostap upstream disclosure: deep-verify + draft report for task 399 survivors
- sqc false-positive feedback: 2 hostap findings that were confirmed TPs but are actually FPs
- Research: full-codebase macro-expansion audit + proper macro-expansion design
- STR31-C relay-function buffer-size resolution: fold same-file relay lookup into FunctionSummary
- STR31-C find_global_buffer_size: AST-based whole-program global-buffer-size join
- MSC13-C: EM_ASM embedded-JavaScript blocks misparsed as C, phantom dead-store variables
- MEM33-C: find_descendant_kind hand-rolled recursive search duplicates lang_parsing_substrate::query primitives already imported in the same file
- Explore C99 ruleset validation options (incl. synthetic conformance corpus)
- MEM33-C: get_function_name / is_pointer_declaration family are additional instances of task 481's named duplication clusters
- Migrate MEM12-C's resource-acquisition list and STR30-C/ENV30-C's printf sublist to call_roles
- float_typing::is_float_type missing C23/GCC extended float types (_Float16/32/64/128, _Decimal32/64/128) that FLP38-C's local is_floating_type already covers
- const_eval macro-merge DRY: fold repeated collect-per-file + merge-context idiom into one helper
- Dedup ALL_CAPS macro-constant-name heuristic (5 sites)
- Work node's real-world checkouts drift off pinned benchmark commits
- todo-sqlite-cli merge driver silently corrupts tasks on cross-node ID collision
- Macro-expansion Phase 4 (optional, opt-in): compile_commands.json preprocessed mode
- Ansible playbook: generate compile_commands.json for Juliet + each real-world benchmark corpus
- sel4: figure out how to generate compile_commands.json for the bare kernel checkout
- Thread --compile-commands through the Juliet + real-world benchmark runners
- Decide and document a cross-rule overlap strategy for CERT-C
- Investigate upstream status: hostap union copy-paste bug in tlsv1_client_read.c, file disclosure if unfixed
- Delta-adjudicate MSC17-C new findings post-task-553 fix (v0.4.260, run #188)
- STR31-C find_fixed_alloc_size: consider adding bare sizeof(T) allocation detection
- API00-C: stop treating assert(ptr != NULL) as validation
- tools_sqc internal utility-layer improvements: remaining items #2/#4/#5/#6/#7/#8
- Delta-adjudicate ARR37-C new findings post-task-556 fix (run #193)
- Delta-adjudicate EXP34-C new findings post-task-558/559 fix (run #193)
- Delta-adjudicate MEM10-C new findings post-task-595 fix (run #193)
- Delta-adjudicate DCL06-C new findings post-task-593 fix (run #193)
- Re-examine task 580's seL4 workaround now that the generated headers can be produced
- Apply the cross-rule overlap decision across existing rules
- bench: realworld_violations duplicate-insertion race in ingest_realworld_run
- install-static-analyzers.yml: libtinfo5 fallback symlink never fires on Ubuntu (rc gate is undefined on apt package-not-found)

## [0.4.123] - 2026-07-23

### Fixed

- Fix call-graph function-boundary corruption from #ifdef-spanning brace mismatch (task 267 follow-up)
- Re-scope task 170 follow-through: group-A noise is fixed by RULE HARDENING, not config-disable (reverted)
- Block-level text-scan robustness: rule heuristics match raw get_node_text spans (comment/string/identifier-substring sensitive)
- Alias-bound CWE-121/122 FP buckets: ARR38-C CWE805 cross-fn dest + ARR30-C CWE-129 subscript

### Changed

- Evaluate whether ratatui/crossterm TUI dependency still earns its keep
- Project-relevance prescan: auto-gate inapplicable rule classes (CON/WIN/C11/Annex-K) per project
- Optimize multi-hop buffer-size propagation: re-parse only forwarder files (cut task-143 cold-scan cost)
- MEM30-C/MEM31-C: explicit-stack rewrite for stateful save/fork/merge dataflow chains (task 261 follow-up)
- DCL13-C alias tracking
- MEM30-C field-level free tracking
- Embed default rules manifest at compile time (cargo install is broken)
- Rename CON rule directory (Windows reserved device name)
- Stop publishing internal dev/research artifacts to crates.io
- Remove unused rfd dependency

## [0.4.84] - 2026-07-08

### Fixed

- Investigate and fix STR31-C false positive patterns
- Shared bounds/buffer-size API in src/analyze/ (consolidation before more FP tasks)
- SECURITY (candidate): sqlite changeset invert/concat OOB-read via sessionChangesetBufferRecord
- Investigate residual INT32-C/INT31-C/INT30-C FPs despite VRA
- MEM30-C freed-context-arg FP suppression (2300 FP / 0 TP in sqlite audit)
- EXP34-C guarded-null FP suppression (1496 FP / 7 TP in sqlite audit)
- API00-C internal-contract FP suppression (1366 FP / 1 TP in sqlite audit)
- Disposition the 37 zero-TP rules from sqlite audit (suppress / gate / raise-evidence)
- Shared root-cause FP: external function-like macro opacity (utlist/uthash) -> EXP33/EXP34
- GATE: sqlite FP reduction before new-codebase expansion
- Shared root-cause FP: free-state precision (free->reassign / conditional / struct-field / loop conflation) -> MEM30/MEM12
- FIO30-C FP investigation and reduction
- Improve CWE-121/CWE-122 flaw-hit rates (cppcheck goal)
- EXP34-C rc==SQLITE_OK / caller-contract FP suppression (~665 sqlite FP)
- MEM30-C realloc-on-field self-assign FP: cfg->topics = realloc(cfg->topics, …) stays flagged
- FN-detection 2a: taint-aware 'untrusted length -> unguarded buffer access' (+ re-point ARR30)
- ARR30 FN 2b: param-decoder index over-read (os_kv kvvfsDecode class)
- ARR30 FN 2b: helper-function over-read across a tainted blob arg (nextchar/readUtf8 class)
- EXP34-C bucket 2: caller-contract / precondition FP suppression (~338 sqlite FP)
- Close CWE-680 gap vs Frama-C
- DCL02-C emits duplicate findings (100x same line)
- POS37-C emits each finding twice (exact duplicate)
- SIG31-C double-emits per shared-object reference instead of per (object,line)
- FIO42-C double-reports the same handle as both unclosed FILE* and unclosed fd
- Upstream raylib Tier-1 bugfix PRs (3 public-API memory-safety bugs)
- Upstream: report raylib IQM-loader missing header validation (OOB on malformed .iqm)
- INT33-C: suppress div-by-zero on FLOAT-typed divisions (operand type inference)
- No-preprocessor: skip inactive #if defined(__cplusplus) blocks (kills DCL40-C/CON03-C C++ FP classes)
- ARR00-C: array-size misparse of braced/designated/{0} initializers -> false OOB
- FLP06-C: suppress 'init with integer arithmetic' when all operands are float
- FLP06-C: residual text-heuristic FP sub-classes (arrow-as-minus, byte-subscript-read, unary-literal, array-brace-init)
- DCL00-C: false 'never modified' on locals reassigned in branches/loops (precision bug)
- MEM30-C: false double-free/UAF from untracked reassignment & container-vs-member frees
- MEM30-C: false double-free from preproc-split if/else-if chains (raudio ctxData, rcore_drm res)
- lang-parsing-substrate 0.1.6 query layer: descendant-search is recursive, not iterative
- Migrate remaining 61 CERT-C rule files off recursive AST descendant-search to substrate::query
- Shared iterative AST walker for rule check() implementations
- Group-B raise-evidence: gate EXP20-C !strcmp idiom + confirm INT33-C bridge.c:930 div-by-zero TP

### Changed

- Trim benchmark MCP server token footprint
- Macro-expansion Phase 0: tree-sitter parse feasibility spike (MACRO(args){block}, preproc_function_def recovery)
- sqlite scope verification result (task 189)
- Macro-expansion Phase 2: in-tree function-like macro expansion engine
- Macro-expansion Phase 3: migrate ARR30 onto shared function_macros (re-scoped post-audit)
- Add TOML validation to build process
- scrape_cert_wiki.py: emit TOML literal strings for description/title to avoid invalid escapes
- Improve test debugging experience
- Add folder nesting to interactive client
- Investigate cross-function buffer-size propagation for STR31-C FP reduction
- Ceiling decision: post-75% strategy — alias analysis vs SSA-lite use-def vs pivot to real-world hardening
- Exclude `#if 0` dead-code blocks from analysis
- Evaluate Melon (Water-Melon/Melon) as benchmark candidate
- MCP realworld server leaks zombie children (doesn't reap)
- Engine-level duplicate-finding backstop in analyze_project
- sqc DB result reports whole-repo c_files/loc, ignoring --exclude
- Refactor analyze_project below AIRD 85
- Refactor run_app below AIRD 85
- Refactor MEM31-C process_call below AIRD 85
- Refactor build.rs generate_integration_tests below AIRD 85
- Refactor MEM31-C analyze_node below AIRD 85
- Refactor create_test_summary_report below AIRD 85
- Refactor MEM30-C scan_function_body below AIRD 85
- Refactor ENV03-C check_writes below AIRD 85
- Refactor analyze_value_ranges below AIRD 85
- Refactor STR31-C check_node below AIRD 85
- Integrate knots complexity gate (pre-commit + CI/CD) + invoke bump-version
- Refactor check_array_subscript below AIRD 85
- Refactor check_return_pointer_arithmetic below AIRD 85
- Refactor render_violations_tab below AIRD 85
- Refactor scan_repository below AIRD 85
- Refactor check_stmt_for_var_assignment below AIRD 85
- Refactor check_dereferences_cfg below AIRD 85
- Refactor analyze_cwe below AIRD 85
- Refactor parse_item_page below AIRD 85
- Refactor infer_type below AIRD 85
- Refactor process_expression_range below AIRD 85
- Refactor check_validation_patterns below AIRD 85
- Refactor analyze_operations below AIRD 85
- Refactor check_strcpy_safety below AIRD 85
- Refactor find_buffer_size below AIRD 85
- Refactor process_call_expression below AIRD 85
- Refactor process_expression below AIRD 85
- Refactor collect_validations below AIRD 85
- Refactor collect_computational_handlers below AIRD 85
- Refactor analyze_null_states_with_globals below AIRD 85
- Refactor process_declaration_null below AIRD 85
- Refactor process_expression_null below AIRD 85
- Refactor check_array_modification below AIRD 85
- Refactor check_function_call below AIRD 85
- Refactor process_declaration below AIRD 85
- Refactor check_buffer_size_mismatch below AIRD 85
- Refactor check_scope_for_shadowing below AIRD 85
- Re-clear knots complexity gate after wheel-edition upgrade (v1.13.1)
- FIO22-C: process_statement still recurses unbounded through nested if/else chains
- DCL01-C: check_scope_for_shadowing recurses unbounded through else-if chains
- DCL30-C: find_static_address_assignment / has_alloc_assignment_in_body residual overflow risk
- INT32-C/INT30-C: resolve_identifier_call_name residual overflow risk via nested if/while/for
- INT33-C: has_early_return_for_zero / collect_assignments_all_nonzero residual overflow risk
- MEM03-C/MEM30-C/MEM31-C: branch-sensitive dataflow analyzers recurse unbounded through nested if/else
- MSC37-C/PRE13-C/STR02-C: statement/path-context recursion residual overflow risk
- ARR38-C: collect_function_definitions_recursive / collect_pointer_offsets_in_node residual overflow risk
- FLP03-C/SIG00-C: branch-feasibility and byte-position pruning residual overflow risk
- API00-C/API02-C: preprocessor/parameter-list pruning recursion left in place
- Record cache warm/cold state on Juliet benchmark runs
- Plumb prescan caching into benchmark runners (make rebuild_prescan real) + test
- Review MSC04-C recall improvement: sqlite3Init indirect-recursion cycle now detected
- Migrate tools_sqc call-graph construction to lang_parsing_substrate::calls (task 296 follow-up)

## [0.4.13] - 2026-06-04

### Fixed

- Add VRA suppression to ARR30-C index bounds checking

### Changed

- Binary distribution packages
- Migrate sqc from Azure DevOps to GitHub
- Third-party licensing and SBOM generation
- Eliminate compiler warnings (clean build generates noise)
- Implement EXP34-C: Do not dereference null pointers
- Implement FIO30-C: Exclude user input from format strings
- Automate mod.rs rule registry generation in build.rs
- Add mutex poisoning recovery
- Optimize runtime performance on real repositories
- Every rule violation should report where and why it failed

## [0.4.0] - 2026-05-07

### Added

- Release v0.2.13
- Release v0.2.15
- Release v0.2.16
- Release v0.2.20
- Release v0.2.21
- Release v0.3.13
- Release v0.3.5
- Release v0.3.8
- Release v0.3.15
- Release v0.3.17
- Release v0.3.18
- Release v0.3.21
- Release v0.3.23
- Release v0.3.24
- Release v0.3.25
- Release v0.3.27
- Release v0.3.28
- Release v0.3.35
- Release v0.3.36
- Release v0.3.43
- Release v0.3.48
- Release v0.3.51
- Release v0.3.56
- Release v0.3.58
- Release v0.3.59
- Release v0.3.60
- Release v0.3.61
- Release v0.3.63
- Release v0.3.64
- Release v0.3.66
- Release v0.3.74
- Release v0.3.75
- Release v0.3.87

### Fixed

- Release v0.2.17
- Release v0.2.22
- Release v0.2.25
- Release v0.3.14
- Release v0.3.3
- Release v0.3.19
- Release v0.3.22
- Release v0.3.26
- Release v0.3.30
- Release v0.3.31
- Release v0.3.32
- Release v0.3.34
- Release v0.3.37
- Release v0.3.38
- Release v0.3.39
- Release v0.3.42
- Release v0.3.57
- Release v0.3.62
- Release v0.3.65
- Release v0.3.67

### Changed

- Release v0.3.20
- Release v0.3.33
- Release v0.3.44
- Release v0.3.46
- Release v0.3.47
- Release v0.3.49
- Release v0.3.50
- Release v0.3.52
- Release v0.3.76
- Release v0.3.77
- Release v0.3.78
- Release v0.3.79
- Release v0.3.80
- Release v0.3.81
- Release v0.3.82
- Release v0.3.83
- Release v0.3.84
- Release v0.3.85
- Release v0.3.86
- Release v0.3.90
- Release v0.3.91
- Release v0.3.92
- Release v0.3.93
- Release v0.3.100
- Release v0.3.101
- Release v0.3.102
- Release v0.3.103
- Release v0.3.97
- Release v0.3.98
- Release v0.3.99
- Release v0.3.104
- API00-C validation look-ahead past variable declarations
- Analysis capabilities roadmap
- DCL19-C / DCL00-C scope and const-qualify FP fixes
- ENV03-C cross-function taint for helper-sink variants
- ENV03-C locally-safe popen/system command var suppression
- EXP02-C extended short-circuit guard recognition
- EXP34-C null safety through if-guards and &stack_var
- INT30-C embedded guard/bound pattern recognition
- INT31-C taint-aware suppression for signed→size_t conversions
- Inter-procedural .c test cases
- MEM05-C / ARR32-C false VLA and stack allocation fixes
- MEM31-C FP reduction for CWE-401
- 49E: ENV03-C v45 global-static pointer write tracking
- 49G: ENV03-C v34 union-field-alias FP suppression
- 49G: ENV03-C v34 union-field-alias FP suppression
- 50G: ENV03-C uppercase macro constant safe strcpy source
- Update task 18 investigation notes: MEM30-C item #8 fixed in v0.3.114
- Miscellaneous embedded FP fixes (small wins)
- INT31-C FP reduction: conditional-branch goodG2B pattern (v08-v17, v6xb)
- DCL07-C + DCL31-C: follow #include chain for declarations; recognize compiler intrinsics
- INT32-C + INT10-C: gate sign-safety rules on operand signedness (unsigned types)
- MEM30-C: gate stack-escape check on pointer/array type of assigned global
- ARR39-C: do not flag sizeof-bounded loop when pointer type is u8*/uint8_t* (element size = 1)
- EXP02-C: allow hardware-flag && countdown-decrement short-circuit pattern
- EXP15-C: do not fire on empty loop body when condition polls a volatile variable
- FLP00-C: only fire on floating-point operands, not integer comparisons
- MEM05-C: verify actual call cycle exists before flagging recursion
- MSC37-C: recognize post-loop unconditional return as valid exit path
- PRE07-C: do not flag trigraph sequences inside // or /* */ comments
- PRE10-C: recognize do-while(0) wrapper spanning multiple lines with backslash continuation


[Unreleased]: https://github.com/brandon-arrendondo/aurora-lint/compare/v0.4.336...HEAD
[0.4.336]: https://github.com/brandon-arrendondo/aurora-lint/compare/v0.4.315...v0.4.336
[0.4.315]: https://github.com/brandon-arrendondo/aurora-lint/compare/v0.4.314...v0.4.315
[0.4.314]: https://github.com/brandon-arrendondo/aurora-lint/compare/v0.4.311...v0.4.314
[0.4.311]: https://github.com/brandon-arrendondo/aurora-lint/compare/v0.4.123...v0.4.311
[0.4.123]: https://github.com/brandon-arrendondo/aurora-lint/compare/v0.4.84...v0.4.123
[0.4.84]: https://github.com/brandon-arrendondo/aurora-lint/compare/v0.4.13...v0.4.84
[0.4.13]: https://github.com/brandon-arrendondo/aurora-lint/compare/v0.4.0...v0.4.13
[0.4.0]: https://github.com/brandon-arrendondo/aurora-lint/releases/tag/v0.4.0
